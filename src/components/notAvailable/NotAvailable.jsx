import './NotAvailable.css';

function NotAvailable (props) {
    return <div className="not-found"> Lo sentimos, no hay hoteles disponibles para los filtros seleccionados : (</div>
}

export default NotAvailable;